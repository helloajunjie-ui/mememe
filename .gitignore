# ===== mememe 公开仓库（程序本体）=====
# 敏感：.env 含 API key
.env
# ===== 私有实例数据（个人状态，绝不放公开仓库；走私有备份）=====
data/memory.db
data/self.yaml
data/methodology.json
data/registry.json
data/registry.json.bak_wordcount
data/snapshots.json
data/env_profile.json
data/sys_profile.json
# 保留 data/persona.yaml（通用人格基座，属于程序框架）
# ===== 本地运行产物（不入云）=====
logs/
sandbox/
backups/
.venv/
workspace/tasks/
workspace/tools_external/
workspace/exam6_download/
workspace/plan_test/
workspace/*.md
# 临时
__pycache__/
*.pyc
data/backup_status.json
data/integrity_baseline.json
data/integrity_status.json

# webui 运行时日志（不入库）
webui/server_*.log
webui/_user_shot.png
config/llm.json
data/context_nodes/
data/session/

config/mcp.json
config/study.yaml
config/workflow_schedule.yaml
data/operation.log
workspace/workflows/
workspace/*.txt
data/ws_write_trace.jsonl

# 凭据库（多账户加密存储，密钥+密文均只留本机）
data/credentials/

# 临时运维脚本（可能含明文凭据，禁止入库）
_ssh*.py
*.pem
*_id_rsa

# 云存档解密密钥（绝不上库）
data/cloud_key.txt
# 私密钥匙区（云存档密钥新址 data/keys/cloud_key.txt；绝不入公开仓库）
data/keys/

# 归档研究产物（本地留存，不入库）
workspace/archive/
data/memory.db.bak*

# 个人形象资产与用户上传（本地留存，不入公开仓库）
persona/
webui/uploads/
*.FBX


# 素月资料库（实例知识，不进公开仓库；走私有备份）
library/

# ===== 实例运行状态（本机状态，不入公开仓库）=====
# 用通配防新增状态文件漏网（如 cloud_backup_status.json 曾漏防）
data/*_status.json
# 工作区废纸篓
workspace/_trash/
# 个人形象资产（webui 头像等；与 persona/ 同策略：本地留存）
webui/assets/
data/last_active.json
# 工作区临时脚本区（一次性/补丁脚本，用完即清，不入库）
workspace/tmp/

# 运行时/实例数据（webui 独立目录）
webui/data/
webui/workspace/
# 编译产物
llm-gateway/*.exe
llm-gateway/_testcfg/

# 凭据/日志/二进制残留
mcp-service/config/secrets.json
mcp-service/data/
*.exe~
llm-gateway/*.exe~
data/restart.flag

# 运行时探测状态（每次扫描都变，不入库）
config/llm_health.json
# ===== local model cache (176MB, never commit) =====
.u2net/

# 第三方依赖（可重新拉取，不入公开仓库；如 archify 图渲染器）
tools/vendor/

# ===== 2026-09-20 补充：工作区大件与备份残留不入库 =====
*.bak_*
workspace/external/
workspace/graphs/
workspace/projects/
workspace/repos/
desktop-pet/
mcp-service/servers/*/bin/
mcp-service/servers/*/node_modules/
mcp-service/servers/*/__pycache__/
mcp-service/servers/*/data/
tools/bin/crw-mcp.exe
mcp-service/servers/urara/

# runtime logs/state (not for repo)
data/crash_log.jsonl
data/llm_errors.jsonl
data/session_state.json
data/mcp_index_history/
# 已装入的第三方 MCP server：克隆体与虚拟环境不入库（装入方式见 mcp-service/config/mcp.json）
mcp-service/servers/microsoft-markitdown/
mcp-service/servers/*/.venv/
mcp-service/workspace/

# 视频抽帧/媒体产物（可重生成，不入公开仓库）
workspace/media/
